<% content_for(:title) do %>Voir une équipe<% end -%>
<p>
	<%= label :name, 'Nom' %><br />
	<%=h @team.name %>
</p>
<p>
	<%= label :users, 'Liste des membres' %><br />
  <% @team.users.each do |user| %>
  <%=h user.login %>
  [ <%= link_to 'Supprimer', user_team_url(:id => @team.id, :user_id => user.id), :method => :delete %> ]
  <br/>
  <% end %>
</p>

<%= link_to 'Modifier', edit_team_path(@team) %> |
<%= link_to 'Retour', teams_path %>
<%= render :partial => 'users/help_box' %>
